# Minimal CI requirements — only what tests actually need
# Installs fast on GitHub Actions (~60s vs 10+ min for full requirements)

# Web framework
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23

# Auth & Security
bcrypt==4.1.1
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Rate limiting & caching (imported by api_server)
slowapi==0.1.9
fastapi-cache2==0.2.2

# Data
pandas==2.1.3
numpy==1.26.2

# ML (minimal — only what test_ml.py needs)
scikit-learn==1.3.2
xgboost==2.0.3

# HTTP client (for TestClient)
httpx==0.25.1
requests==2.31.0

# Env
python-dotenv==1.0.0

# Reporting
reportlab==4.0.7

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
